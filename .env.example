# Database Configuration
# PostgreSQL connection settings
DB_NAME=rand_refinary
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432

# Alternative: Full Database URL (for cloud deployments like Render, Heroku)
# Uncomment and use this for production deployments
# DATABASE_URL=postgresql://username:password@host:port/database_name

# Flask Configuration
SECRET_KEY=your_secret_key_here_change_this_in_production
FLASK_ENV=development
FLASK_APP=app.py
FLASK_DEBUG=True

# Application Settings
SESSION_TYPE=filesystem
PERMANENT_SESSION_LIFETIME=3600

# GPS/Location Settings (if needed)
# GPS_ENABLED=True
# GPS_PORT=/dev/ttyUSB0

# Email Settings (if needed for notifications)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your_email@example.com
# MAIL_PASSWORD=your_email_password
